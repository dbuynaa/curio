#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

exec "$basedir/../node_modules/@pnpm/exe/pnpm"   "$@"
exit $?
# cmd-shim-target=/Users/buynaadamdinsuren/Documents/GitHub/curio/.pnpm-store/v11/links/@pnpm/exe/10.19.0/b92621d7988564d7c77879cb7206d597ed90ce7d1b82ebc2aa039743553f8b4a/node_modules/@pnpm/exe/pnpm
